package com.flexfit.workoutplanservice.model.enums;

// A new enum to track the status of workouts and exercises.
public enum CompletionStatus {
    PENDING,
    COMPLETED,
    SKIPPED,
    IN_PROGRESS
}